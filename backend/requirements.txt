# runtime deps for the API. Exact pins so every build installs the same thing;
# Dependabot opens a PR when a new version is out.
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
sqlalchemy[asyncio]==2.0.54
asyncpg==0.31.0
aiosqlite==0.22.1
alembic==1.20.0
httpx==0.28.1
redis==8.1.0
PyJWT==2.15.0
python-dotenv==1.2.3
anyio==4.15.1
